#include "HSV.compute"
#pragma kernel PatternKernel

uint spectrumSize;
float hueOffset;
float4 spectrumData[256];
float gradStart = 0.25f;
float gradEnd = 0.6f;
float height;
float width;
#define BLACK float4(0,0,0,1)
//#define TRANSPARENT float4(.5,.55,1,1);
#define TRANSPARENT float4(0,0,0,0);
RWTexture2D<float4> outputTex;

Texture2D<float4> gradientTex;
SamplerState samplergradientTex;

[numthreads(16, 16, 1)]
void PatternKernel(uint3 id : SV_DispatchThreadID)
{
	uint2 position = id.xy;
	uint spectrumIndex = position.y;
	
	float spectrumFloatIdx = position.y / (height - 1);
	int scaledSpectrumIndex = spectrumFloatIdx * (spectrumSize-1);

	// This indexing strategy is required due to the way shader architecture packs floats
	float binValue = spectrumData[scaledSpectrumIndex >> 2][scaledSpectrumIndex & 3];
	
	float barHeight = lerp(0, 128, binValue * 2);
	
    float t = spectrumFloatIdx;
    float4 color = gradientTex.SampleLevel(samplergradientTex, float2(t, 0.5), 0);

	float4 result = id.x < barHeight ? color : TRANSPARENT;
    outputTex[id.xy] = result;
}